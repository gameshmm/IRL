# Copie este arquivo para .env e preencha os valores
# cp .env.example .env

# ─── Segurança (OBRIGATÓRIO mudar em produção) ────────────────────────────────
JWT_SECRET=troque_esta_chave_para_uma_string_aleatoria_longa

# ─── Portas (opcional — padrões já definidos no docker-compose.yml) ───────────
# API_PORT=3001
